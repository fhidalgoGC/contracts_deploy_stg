export interface NavBarProps {
  title: string;
}

export interface Breadcrumb {
  label: string;
  path: string;
  isActive: boolean;
}

export interface Language {
  code: string;
  name: string;
  flag: string;
}